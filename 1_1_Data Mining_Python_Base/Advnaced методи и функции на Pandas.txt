Advnaced методи и функции на Pandas
Разширени методи и функции на Pandas и DataFrame
1. Групиране и агрегиране:

📊 groupby(): Разделя данните на групи въз основа на зададени критерии.
📈 agg(): Извършвайте операции по агрегиране на групираните данни (например сума, средна стойност, брой).
📊 pivot_table(): Създаване на въртящи се таблици за обобщаване на данни.
📊 crosstab(): Изчисляване на кръстосана табулация на два или повече фактора.
2. Сливане и обединяване:

🔗 merge(): Комбинирайте DataFrames, като използвате различни видове обединения (вътрешни, външни, леви, десни).
🧩 concat(): Свързване на DataFrame по редове или колони.
3. Преоформяне на данни:

📁 melt(): Разгънете или разтопете DataFrame от широк в дълъг формат.
🔀 stack() и unstack(): Преместване на нивото(ята) на етикетите на индексите към колоните и обратно.
4. Анализ на времеви редове:

🕒 resample(): Преизчисляване на данни от времеви редове (например от дневни към месечни) с помощта на функции за агрегиране.
🔄 rolling(): Изчисляване на статистически данни за движението (напр. средна стойност за движението, сума за движението).
5. Обработка на липсващи данни:

❓ isna() и notna(): Проверка за липсващи стойности.
🔄 fillna(): Попълнете липсващите стойности, като използвате различни методи (напр. forward fill, backward fill).
🗑️ dropna(): Премахване на редове или колони с липсващи стойности.
6. Манипулация на низове:

🔍 str.contains(): Проверка за съдържане на низ.
✏️ str.replace(): Замяна на поднизове в колони с низове.
🔍 str.extract(): Извличане на определени модели от низове.
7. Функции Apply и Map:

📝 apply(): Прилагане на потребителска функция към елементи на DataFrame или редове/колони.
🗺️ map(): Съпоставяне на стойности в серия към нови стойности въз основа на речник или функция.
8. Комбиниране на данни:

↕️ combine_first(): Комбинирайте две DataFrame, като предпочитате стойностите от първата, докато запълвате със стойности от втората.
9. Обработка на категорични данни:

📊 astype(): Промяна на типа данни на серия.
📊 cut() и qcut(): Разпределете данните в дискретни интервали или квантили.
10. Потребителски функции за агрегиране:
- 🧮 Дефинирайте потребителски функции за агрегиране с помощта на agg() за сложни агрегации, които не са обхванати от вградените функции.

11. Оформяне на рамки от данни:
- 🎨 Използвайте df.style, за да прилагате условно форматиране и стилизиране на DataFrame за по-добра визуализация.

12. Многократно индексиране:
- 🏢 Създавайте и работете с DataFrames, които имат няколко нива на индекси на редове и колони за йерархични данни.

13. Оптимизиране на паметта:
- 💾 Използвайте техники като astype() с типа данни pd.Categorical, за да намалите използването на паметта за категорични данни.